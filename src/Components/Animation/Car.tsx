import * as React from 'react';
import './Car.css';
interface ICar {
}

export const Car: React.FC<ICar> = () => {
    return (
        <section className='Car'>
            
        </section>
    );
}